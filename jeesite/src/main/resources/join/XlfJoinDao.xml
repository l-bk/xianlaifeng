<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.join.dao.XlfJoinDao">
    
	<sql id="xlfJoinColumns">
		a.id AS "id",
		a.methodid AS "methodid",
		a.actid AS "actid",
		a.userid AS "userid",
		a.jointime AS "jointime",
		a.joinstatus AS "joinstatus"
	</sql>
	
	<sql id="xlfJoinJoins">
	</sql>
    
	<select id="get" resultType="XlfJoin">
		SELECT 
			<include refid="xlfJoinColumns"/>
		FROM xlf_join a
		<include refid="xlfJoinJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="XlfJoin">
		SELECT 
			<include refid="xlfJoinColumns"/>
		FROM xlf_join a
		<include refid="xlfJoinJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="XlfJoin">
		SELECT 
			<include refid="xlfJoinColumns"/>
		FROM xlf_join a
		<include refid="xlfJoinJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO xlf_join(
			id,
			methodid,
			actid,
			userid,
			jointime,
			joinstatus
		) VALUES (
			#{id},
			#{methodid},
			#{actid},
			#{userid},
			#{jointime},
			#{joinstatus}
		)
	</insert>
	
	<update id="update">
		UPDATE xlf_join SET 	
			methodid = #{methodid},
			actid = #{actid},
			userid = #{userid},
			jointime = #{jointime},
			joinstatus = #{joinstatus}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM xlf_join
		WHERE id = #{id}
	</update>
	
</mapper>